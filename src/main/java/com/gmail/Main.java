package com.gmail;

/*
  You have the boolean method isFalse() which has to return false, but returns true.

  Place only the one pair of parentheses in the expression:

   a && b || c && !d || a
   given that a = true, b = false, c = false, d = true so the method starts returning false.
*/

public class Main {
    public static void main(String[] args) {

        System.out.println(isFalse(true,false,false,true));

    }
    public boolean booleanExpression(boolean a, boolean b, boolean c, boolean d) {
        //write code below this line
        return false;
        //write code above this line
    }
}
